{
  "name": "radiuscheck",
  "version": "1.0.0",
  "description": "check that radius is still  updating",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemailer": "^3.0.2"
  }
}
